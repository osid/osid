<h2>OSID news archive</h2>

<%= will_paginate @blogposts %>

<table>
		<col width="700">
		<col width="100">
		<col width="100">
<% @blogposts.each do |post| %>
		 	<tr>
				<td><b><%= post.header %></b></td><td><%= post.updated_at.strftime("%Y-%m-%d") %></td><td>by: <%= post.user.name %></td>
			</tr>
			<tr>
				<td colspan="3"><br><%= post.content %><br><br><br></td>
			</tr>
<% end %>
</table>

<%= link_to "RSS Feed", "/feed" %>

<br />